using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class s_groupSelect : s_menucontroller
{
    public List<s_enemyGroup> enemyGroup;
    public o_locationOverworld locOverworld;

    public override void OnOpen()
    {
        base.OnOpen();
        ResetButton();
        for (int i = 0; i < enemyGroup.Count; i++) {
            s_enemyGroup enGr = enemyGroup[i];
            s_groupSelectButton b = GetButton<s_groupSelectButton>(i);
            b.gameObject.SetActive(true);
            b.group = enGr;
            b.txt.text = enGr.name;
        }
    }
}
*/